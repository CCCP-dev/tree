# 页面使用说明

这份文档说明 `war-thunder-tree` 项目里每个 HTML 文件的用途。

## `index.html`

项目首页，也是进入整个科技树网站的入口。

用途：

- 展示海军、陆军、空军三个分支入口。
- 当前陆军入口可用，点击后进入 `陆军.html`。
- 海军和空军目前是占位入口，点击后会弹出“资料尚未录入”的提示。
- 页面下方有编辑器入口，可以进入 `editor.html`。

适合使用场景：

- 普通浏览者第一次打开网站。
- 从这里选择要看的军种科技树。
- 从这里进入科技树编辑器。

## `陆军.html`

陆军科技树总览页面。

用途：

- 展示陆军载具研发树。
- 支持拖动空白区域平移视图。
- 支持鼠标滚轮缩放视图。
- 支持搜索载具名称。
- 点击某个载具节点后，会进入该载具的详情页 `载具.html?id=...`。
- 支持多人/多用户标记功能，用来标出不同用户关注或拥有的载具。
- 可以一键清除标记。

适合使用场景：

- 查看陆军载具之间的研发关系。
- 搜索某个坦克在科技树中的位置。
- 点击坦克进入单车主页。
- 标记自己或朋友正在研发、已拥有、想购买的载具。

## `载具.html`

单个载具详情页，也就是每辆坦克自己的主页。

用途：

- 根据地址里的 `id` 参数读取对应载具。
- 显示载具名称。
- 显示对应的 War Thunder Wiki 链接。
- 如果该载具有对应图片，会自动显示 `images` 文件夹里的 X-ray 俯视图。
- 显示该载具的前置载具。
- 显示该载具的后续载具。
- 提供返回陆军科技树和返回首页的按钮。

使用方式：

```text
载具.html?id=载具ID
```

例如：

```text
载具.html?id=KluoZMAlmy467076
```

图片对应方式：

- 页面会读取 `js/vehicle-images.js`。
- `js/vehicle-images.js` 里保存了 Wiki 链接到图片路径的映射。
- 图片实际放在 `images` 文件夹中。

适合使用场景：

- 查看某辆坦克的单独信息。
- 查看这辆车的 X-ray 图片。
- 从当前车跳转到前置或后续研发车辆。

## `editor.html`

科技树编辑器页面。

用途：

- 用来编辑科技树数据。
- 可以选择编辑的军种。
- 可以搜索节点。
- 可以新增载具节点。
- 可以删除当前选中的载具节点，相关连线会一起删除。
- 可以编辑节点名称。
- 可以编辑节点的 Wiki 链接。
- 可以给节点添加一张或多张图片。
- 可以拖动车辆节点来调整布局位置。
- 可以新增和查看节点之间的连线。
- 可以导出草稿。
- 可以确认并生成新版本。

注意：

- 这是编辑用页面，不是普通浏览主页。
- 修改前建议先备份现有数据。
- 如果只是看科技树，不需要打开这个页面。

适合使用场景：

- 添加新坦克。
- 删除不需要的坦克节点。
- 修改坦克名称或 Wiki 链接。
- 给坦克主页添加图片或多张展示图。
- 调整科技树节点位置。
- 修改研发连接关系。
- 批量整理科技树数据。

## 页面之间的关系

```text
index.html
  ├─ 陆军.html
  │   └─ 载具.html?id=某个载具ID
  └─ editor.html
```

简单理解：

- `index.html` 是入口。
- `陆军.html` 是陆军科技树地图。
- `载具.html` 是每辆坦克的详情页。
- `editor.html` 是编辑科技树用的工具页。

## 相关资源文件

虽然这些不是 HTML 页面，但它们会支撑页面运行：

- `css/styles.css`：全站主要样式。
- `css/editor.css`：编辑器专用样式。
- `js/data.js`：科技树数据。
- `js/core.js`：科技树通用逻辑。
- `js/home.js`：首页交互逻辑。
- `js/army.js`：陆军科技树交互逻辑。
- `js/vehicle.js`：载具详情页逻辑。
- `js/vehicle-images.js`：载具图片映射表。
- `images`：存放每辆坦克的 X-ray 图片。

## 推荐打开顺序

普通浏览：

```text
index.html → 陆军.html → 载具.html
```

编辑维护：

```text
editor.html → 导出/确认数据 → 检查 陆军.html 和 载具.html
```
