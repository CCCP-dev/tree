# War Thunder 真实 X-ray 制作与放置说明

这份文档是本项目后续维护者和 AI 代理的标准操作手册。

## 0. 最重要的规则

- 必须优先使用《War Thunder》本机游戏资源制作图片。
- 不要使用 AI 绘图、网络坦克图片或凭空绘制的内构替代真实资源。
- 真实资源通常位于：
  `D:\WarThunder\content\base\res\tanks`
- 项目目录是：
  `C:\Users\启元\Desktop\war-thunder-tree`
- 工作脚本和临时导出脚本位于：
  `C:\Users\启元\Desktop\c++`
- 最终网页图片必须放在：
  `C:\Users\启元\Desktop\war-thunder-tree\images`
- 本项目采用俯视、半透明、彩色模块化 X-ray 风格，不是原始游戏纹理截图。

如果用户说“按照之前的方法”“利用战争雷霆的文件”“真实内构”，必须执行本手册的 `.grp -> OBJ -> X-ray 合成 -> 主页绑定` 流程。

## 1. 工具和输入文件

### 1.1 必需工具

- `C:\Python312\python.exe`
- Python Pillow，渲染器使用 `PIL`
- Dagor Asset Explorer 的 Python 源码：
  `C:\Users\启元\Desktop\c++\tools\DagorAssetExplorer\src\src\dae`
- Windows PowerShell

Dagor Asset Explorer 不一定要启动图形界面。实际批量流程使用它的 Python 解析器读取 `.grp`。

### 1.2 游戏资源

游戏资源是 Dagor 格式的 `.grp` 文件，例如：

```text
D:\WarThunder\content\base\res\tanks\usa_m36_slugger.grp
D:\WarThunder\content\base\res\tanks\cn_qn506.grp
```

每个资源包里通常包含：

- 基础外观 `DynModel`
- 内构模型，名称通常以 `_xray` 结尾
- 损伤模型，名称通常包含 `_dmg`
- 其他特殊配置或变体

制作标准 X-ray 时，选择：

1. 与车辆名称最匹配的基础外观模型；
2. 与车辆名称最匹配的 `_xray` 模型；
3. 不选择 `_dmg` 模型作为正常车体；
4. 不要因为名称更长就盲目选择特殊变体。

例如 `usa_m36b2.grp` 中可能同时有：

```text
m36b2
m36b2_cefeo
m36b2_dmg
m36b2_cefeo_dmg
m36b2_xray
m36b2_cefeo_xray
```

如果节点是普通 M36B2，应使用 `m36b2 + m36b2_xray`，而不是自动选择 `m36b2_cefeo`。

## 2. 从科技树确认目标车辆

先读取活动数据：

```text
C:\Users\启元\Desktop\war-thunder-tree\js\data.js
```

查找节点的：

- `id`
- `name`
- `wikiUrl`
- `images`

例如 QN506 节点应类似：

```json
{
  "id": "new_node_11",
  "name": "QN506",
  "wikiUrl": "https://wiki.warthunder.com/unit/cn_qn506",
  "images": []
}
```

`js/data.js` 是当前网页使用的数据源。不要只查看旧的 `js/data.text.js`，因为它可能是较早的快照，未包含后来新增的节点。

如果目标节点没有独立 `wikiUrl`，仍然可以使用真实 `.grp` 制作图片，图片应直接写入该节点的 `images` 数组。

## 3. 查找对应 `.grp`

先按 Wiki URL 最后一段、节点名称和常见别名搜索：

```powershell
Get-ChildItem -LiteralPath 'D:\WarThunder\content\base\res\tanks' `
  -Recurse -File -Filter '*.grp' |
  Where-Object { $_.Name -match '(?i)qn506|m36|churchill' }
```

不要只根据中文名称猜资源文件名。优先级如下：

1. Wiki URL 中的内部名称；
2. 已有资源索引；
3. `.grp` 文件名；
4. 车辆国家、底盘和型号作为辅助判断。

如果没有独立资源：

- 先报告没有找到独立 `.grp`；
- 只有用户明确接受时，才使用同系列真实车辆作为 fallback；
- fallback 必须在交付说明中注明，不能伪装成完全对应的独立模型。

## 4. 导出真实 OBJ

### 4.1 推荐脚本结构

导出脚本需要完成以下工作：

1. 加载 Dagor Asset Explorer Python 模块；
2. 打开目标 `.grp`；
3. 缓存 `GeomNodeTree`、`CollisionGeom`、`DynModel` 和 `RendInst`；
4. 找出基础外观和 `_xray`；
5. 分别导出 OBJ；
6. 记录实际选择的模型名称。

已有脚本示例：

```text
C:\Users\启元\Desktop\c++\make_qn506_real_xray.py
C:\Users\启元\Desktop\c++\make_m36_real_xray.py
C:\Users\启元\Desktop\c++\make_churchill_real_xray.py
C:\Users\启元\Desktop\c++\make_wartree_real_xray.py
```

单车脚本的命令形式：

```powershell
& 'C:\Python312\python.exe' `
  'C:\Users\启元\Desktop\c++\make_qn506_real_xray.py'
```

导出的临时 OBJ 通常放在：

```text
C:\Users\启元\Desktop\wartree-x-ray\<vehicle>-objects\<vehicle>\
```

项目内已经归档的批量资源位于：

```text
C:\Users\启元\Desktop\war-thunder-tree\real-xray-source\objects-real\
```

### 4.2 中文 Windows 路径问题

Python 直接使用 `Path.home()` 或脚本中硬编码中文用户名时，某些终端编码可能把用户名变成乱码。可靠做法是使用 Windows Unicode API 获取 `USERPROFILE`，或使用 ASCII 路径别名。

推荐：

```python
import ctypes
from pathlib import Path

buffer = ctypes.create_unicode_buffer(32768)
length = ctypes.windll.kernel32.GetEnvironmentVariableW(
    "USERPROFILE", buffer, len(buffer)
)
home = Path(buffer.value) if length else Path.cwd().parent.parent
```

不要把类似 `C:\Users\启元` 经过错误编码后写成固定乱码字符串。

## 5. 使用现有 X-ray 合成器

标准渲染器：

```text
C:\Users\启元\Desktop\c++\render_xray_composite_2d.py
```

核心函数：

```python
from render_xray_composite_2d import render_composite

render_composite(
    hull_obj,
    xray_obj,
    output_image,
    width=1280,
    height=900,
)
```

命令行形式：

```powershell
& 'C:\Python312\python.exe' `
  'C:\Users\启元\Desktop\c++\render_xray_composite_2d.py' `
  '<hull.obj>' `
  '<vehicle_xray.obj>' `
  'C:\Users\启元\Desktop\war-thunder-tree\images\<name>.jpg'
```

### 5.1 渲染层级

渲染器从 OBJ 的 `g` 分组名称判断模块类别，主要顺序如下：

1. 深色背景和网格；
2. 半透明蓝色车体轮廓；
3. 普通内部模块；
4. 炮塔、炮管和武器结构；
5. 乘员层，乘员必须最后绘制。

这一步很重要：乘员如果被炮塔、炮管或模块压住，就必须检查 `render_xray_composite_2d.py` 中的 crew layer，确保它在所有其他层之后合成。

### 5.2 颜色规则

当前标准颜色大致为：

- 乘员：蓝色；
- 弹药：黄色；
- 发动机、变速箱、散热器：橙色；
- 燃油：红色；
- 炮管、炮闩、火炮结构：浅色；
- 装甲和外壳：低透明度蓝色或暗红色；
- 其他模块：灰色或棕色低透明度。

目标不是把所有模块画成不透明色块。模块重叠时应保留透明度，使车体轮廓、乘员和内部结构同时可读。

## 6. 特殊载具和组合模型

如果用户要求“车体用 A、炮塔用 B、炮管用 C”，不能只在文字上说明，必须从真实 OBJ 中组合。

基本方法：

1. 读取各 OBJ 的顶点、面和 `g` 分组；
2. 从车体 OBJ 保留车身、履带和底盘组；
3. 从炮塔 OBJ 保留 `turret`、`bone_turret`、`gun`、`barrel`、`cannon` 等组；
4. 从 X-ray OBJ 分别保留车体模块和炮塔/乘员模块；
5. 写出新的组合 OBJ；
6. 用同一个 `render_composite` 渲染。

组合前要确认各模型的坐标原点和朝向一致。若不一致，必须先检查边界和顶点位置，不能直接叠加。

如果真实游戏文件里不存在用户要求的特定炮管：

- 先报告缺失；
- 可以从同游戏、同口径的真实模型中提取，但要说明来源；
- 不要使用 AI 生成的炮管或内构；
- 不能把“视觉上相似”说成“游戏原始模型完全一致”。

## 7. 图片命名和放置

最终图片放入：

```text
C:\Users\启元\Desktop\war-thunder-tree\images
```

推荐使用稳定、易读、与节点关联的文件名：

```text
images/qn506.jpg
images/m36.jpg
images/m36b3.jpg
images/churchill_iii.jpg
```

如果同一车辆需要多张图，可以使用：

```text
images/qn506.jpg
images/qn506_side.jpg
images/qn506_detail.jpg
```

不要只把图片放入临时 `wartree-x-ray` 文件夹而不复制到项目 `images` 目录。网页只读取项目内的图片。

## 8. 绑定到车辆主页

### 8.1 推荐方式：节点自己的 `images`

在 `js/data.js` 对应节点中写入相对路径：

```json
{
  "id": "new_node_11",
  "name": "QN506",
  "wikiUrl": "https://wiki.warthunder.com/unit/cn_qn506",
  "images": ["images/qn506.jpg"]
}
```

多张图片：

```json
"images": [
  "images/qn506.jpg",
  "images/qn506_side.jpg"
]
```

`js/vehicle.js` 会优先读取节点的 `images`。因此新添加或特殊组合车辆应优先使用这种方式。

### 8.2 旧方式：`js/vehicle-images.js`

对于大量已有标准车辆，也可以使用 Wiki URL 映射：

```js
{
  "https://wiki.warthunder.com/unit/cn_qn506":
    "images/qn506.jpg"
}
```

使用旧映射时必须确认：

- URL 完全一致；
- 图片路径相对于项目根目录；
- 图片文件确实存在；
- 不会被节点自己的 `images` 覆盖。

不要为了新节点同时写两套不同图片来源，除非确实需要 fallback。

## 9. 编辑器中的图片操作

打开：

```text
editor.html
```

选中节点后，在载具图片路径区域填写一行一个路径：

```text
images/qn506.jpg
images/qn506_side.jpg
```

也可以使用文件选择器一次选择多张图片。

注意：

- 编辑器中的路径应使用 `/`；
- 路径应是项目相对路径；
- 图片本身仍需实际复制到 `images` 目录；
- 生成新数据后要检查 `js/data.js` 是否真的保存了 `images` 数组；
- 不要只修改浏览器内存中的数据。

## 10. 批量制作流程

批量流程使用：

```text
C:\Users\启元\Desktop\c++\make_wartree_real_xray.py
```

它的工作顺序是：

1. 读取车辆索引清单；
2. 为每辆车匹配 `.grp`；
3. 选择外观 DynModel；
4. 选择 `_xray` DynModel；
5. 导出 OBJ；
6. 调用 `render_composite`；
7. 写入生成报告；
8. 写入缺失报告。

相关输出：

```text
C:\Users\启元\Desktop\wartree-x-ray\objects-real
C:\Users\启元\Desktop\wartree-x-ray\images-real
C:\Users\启元\Desktop\wartree-x-ray\real-xray-generated.json
C:\Users\启元\Desktop\wartree-x-ray\real-xray-missing.json
C:\Users\启元\Desktop\wartree-x-ray\real-xray-export.log
```

重新渲染已有 OBJ，不重新导出模型时使用：

```text
C:\Users\启元\Desktop\c++\rerender_wartree_real_xray.py
```

修改颜色、透明度或层级后，优先重新运行渲染，不要重复导出所有 `.grp`。

## 11. 人工替代和缺失资源

项目历史上允许某些没有独立资源的节点使用相近真实车辆替代，例如：

- 缺失型号使用同底盘同炮塔车辆；
- 用户明确指定用 KV-1E、KV-2、T-34-85 等已有模型；
- 特殊科技树节点使用多个真实车辆的 OBJ 组合。

每次替代必须记录：

```text
目标节点：
实际使用的 grp：
实际使用的 hull 模型：
实际使用的 xray 模型：
是否为 fallback：
```

不要把 fallback 图片写成目标车辆的“原始游戏内构”，应在交付说明或报告中注明。

## 12. 检查清单

完成一辆车后，必须检查：

- [ ] 节点确实存在于 `js/data.js`；
- [ ] 节点的 `wikiUrl` 或 `id` 已确认；
- [ ] 找到了正确的 `.grp`；
- [ ] 导出了基础外观 OBJ；
- [ ] 导出了对应 `_xray` OBJ；
- [ ] 没有误用 `_dmg`；
- [ ] 图片由真实 OBJ 渲染，不是 AI 生成；
- [ ] 车体轮廓完整；
- [ ] 炮塔和炮管存在；
- [ ] 乘员可见且在最上层；
- [ ] 弹药、发动机和燃油模块没有全部互相遮死；
- [ ] 图片尺寸通常为 `1280x900`；
- [ ] 图片已复制到项目 `images` 目录；
- [ ] 节点 `images` 字段指向正确相对路径；
- [ ] 浏览器详情页能够加载图片；
- [ ] `tests/vehicle.test.js` 通过。

PowerShell 可进行基本文件检查：

```powershell
Test-Path 'C:\Users\启元\Desktop\war-thunder-tree\images\qn506.jpg'
```

运行车辆页测试：

```powershell
node --test `
  'C:\Users\启元\Desktop\war-thunder-tree\tests\vehicle.test.js'
```

## 13. 常见错误

### 图片全黑或没有车体

可能原因：

- 传入了错误 OBJ；
- OBJ 没有 `g` 或 `f` 面；
- 使用了只有内构、没有外观的模型；
- 坐标边界计算失败。

先检查 OBJ 中是否存在：

```text
v ...
g ...
f ...
```

### 只有模块，没有车体轮廓

说明只渲染了 `_xray` OBJ，或基础外观 OBJ 选择错误。必须同时传入：

```text
hull.obj
vehicle_xray.obj
```

### 乘员被炮塔压住

检查渲染器是否最后执行了 crew layer。乘员过滤关键字包括：

```text
commander
gunner
loader
driver
crew
```

### 炮塔或炮管消失

不要把基础外观中的武器组过滤掉后就结束。渲染器需要额外绘制：

```text
turret
gun
barrel
cannon
breech
mantlet
```

### Python 找不到中文用户目录

改用 Windows Unicode API 获取 `USERPROFILE`，不要在脚本中使用乱码路径。

### 页面不显示图片

依次检查：

1. 图片是否在项目 `images` 目录；
2. `images` 路径是否使用 `/`；
3. `js/data.js` 的节点是否真的有 `images`；
4. 当前打开的页面是否加载 `js/data.js`；
5. 浏览器是否缓存旧页面；
6. `vehicle.js` 是否优先读取了另一个旧映射。

## 14. AI 执行摘要

后续 AI 接到“给某辆车制作 X-ray”时，应按以下顺序执行：

1. 读取 `js/data.js`，确认节点 ID、名称和 Wiki URL；
2. 在 `D:\WarThunder\content\base\res\tanks` 查找对应 `.grp`；
3. 先确认资源确实包含基础 DynModel 和 `_xray` DynModel；
4. 导出正确的外观 OBJ 和 `_xray` OBJ；
5. 使用 `render_xray_composite_2d.py` 合成 `1280x900` JPG；
6. 目视检查车体、炮塔、炮管、模块和乘员层级；
7. 将图片复制到项目 `images` 目录；
8. 在 `js/data.js` 对应节点的 `images` 数组中写入相对路径；
9. 不使用 AI 绘图替代真实游戏资源；
10. 运行文件检查和 `tests/vehicle.test.js`；
11. 最后报告实际使用的 `.grp`、模型名、输出图片和是否存在 fallback。
